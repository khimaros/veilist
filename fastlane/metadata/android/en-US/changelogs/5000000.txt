* fixed edits made in quick succession reaching other devices as only the first
  one, while this device showed them all and said "synced"
* after losing signal or leaving airplane mode, the app gets itself back on the
  network instead of sitting disconnected until you restart it
* edits made while offline go out when you next open the app, even if the app
  was in the background when the network came back
* fixed a list emptying itself: after everyone had been away for a while the
  app could no longer read the shared list, said "synced" anyway, and the next
  edit published an empty list over everyone's items
* your own items are now kept on your device as well as in the shared list, and
  a list nobody edits is refreshed in the background so it stays reachable
* a list someone else has changed since you last looked at it is marked on the
  listing, so you can see what moved without opening each one
* every list catches up at once when you open the app, instead of one after
  another, so a big roster no longer takes a while to be current
* fixed a list shared with you being read-only while offline until you had
  edited it at least once with a connection
